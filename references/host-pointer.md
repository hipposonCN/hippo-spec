# Host pointer

This is the only always-on Hippo Spec text. It does not grant push, merge, or production permission. Project AGENTS, specs, feature maps, tests, and CI stay in the project.

Use it in the host's existing short-entry file (`AGENTS.md`, a Cursor user rule, `CLAUDE.md`). Do not paste `SKILL.md` or the reference tree into that file.

```text
For substantive implement, repair, resume, review, or delegate work, read
the installed Hippo Spec SKILL.md, then only the one method reference and
one host adapter that file names. Also read the project's AGENTS, effective
task/spec, and relevant feature-map entries.

State scope, owner, baseline, acceptance, and the authorized delivery
target. The original owner closes that target.

A clear local edit with no behavior-contract delta stays skip: make the
change and run the relevant check. Do not load Hippo Spec references.
```

If the turn is `skip`, stop here. Do not open the kernel, a method reference, or a host adapter.
