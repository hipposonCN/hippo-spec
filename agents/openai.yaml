interface:
  display_name: "Hippo Spec"
  short_description: "短内核完成有界改动；按需读取验证、地图维护与当前宿主适配器"
  default_prompt: "使用 $hippo-spec 按现有规范完成本轮目标。先选最轻模式；skip 不加载 reference。需要 PR/CI/浏览器时先读 generic-delivery，再读当前宿主适配器。"
